# Handoff — 2025-10-12 Performance Sprint

_Last updated: 2025-10-12 by Codex_

## Current State
- **Phase 1 (Telemetry)** — Complete. Baseline metrics logged in `docs/research/artifacts/2025-10-12-phase-1/` and summary captured in `docs/research/README.md`.  
- **Phase 2 (Bundle Diet)** — Complete. `astro-tooltips` and `fuse.js` removed; search island slimmed; Tabs shortcode rewritten. Hydrated JS total now 169 kB. Details: `docs/research/artifacts/2025-10-12-phase-2/`.  
- **Phase 3 (Asset Optimization)** — Complete with caveat. Responsive `sizes`, async decoding, and hero fetch priorities shipped; background imagery now serves AVIF/WebP/JPG via `image-set`. Fonts preloaded (Inter 400/700, Cormorant 500/700). LCP improvements landed on desktop but mobile still shows ~3.9 s due to H1 layout (see “Open Items”).
- **Prefetch Strategy** — Switched Astro’s `prefetchAll` to `defaultStrategy: "hover"` so idle mobile sessions don’t prefetch every link (`astro.config.mjs`). Does not yet resolve mobile LCP regression.

## Latest Metrics (static `npx serve`, 2025-10-12)
| Page | Device | Perf | FCP | LCP | CLS | TBT | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `/` | Desktop | 100 | 0.40 s | 0.80 s | 0 | 0 ms | ✅ target hit |
| `/` | Mobile | 87–88 | 1.42 s | 3.92–3.94 s | 0 | 0 ms | LCP element = hero `<h1>` |
| `/sound-healing-nepal/learn` | Desktop | 100 | 0.41 s | 0.73 s | 0 | 0 ms | ✅ |
| `/sound-healing-nepal/learn` | Mobile | 88 | 1.40 s | 3.90 s | 0 | 0 ms | H1 again the LCP |

Full JSON + logs: `docs/research/artifacts/2025-10-12-phase-3/`.

## Key Changes to Note
- `astro.config.mjs` — `prefetch.defaultStrategy` set to `"hover"`.  
- Hero/service components (`src/components/home/EntryLayout.astro`, `src/components/services/EntryLayout.astro`, etc.) now pass `sizes`, `decoding="async"`, and `fetchpriority="high"`.  
- `src/components/base/Background.astro` uses `image-set` for AVIF/WebP/JPEG combos; small screens fall back to solid color.  
- `src/components/base/BaseLayout.astro` preloads Cormorant 500 alongside existing fonts.  
- Asset optimizations stored in `public/images/` (new AVIF/JPG files) with originals transformed via `sharp`.  
- Documentation updated: `docs/research/README.md`, `docs/seo/experiments.md`, and `docs/research/2025-10-12-phase-3-spec.md`.

## Open Items / Recommendations
1. **Mobile LCP Regression** — Investigate hero heading render cost. Ideas:  
   - Test setting Cormorant headings to `font-display: optional` or swap mobile hero heading to Inter (already preloaded).  
   - Evaluate making the hero background gradient-only for mobile to reduce paint cost.  
   - Re-run Lighthouse after each tweak; aim for ≤2.5 s LCP on `/`.
2. **Phase 4 (Styling Surgery)** preparation:  
   - Audit SCSS/Tailwind output to remove unused utilities.  
   - Explore extracting critical CSS for hero/above-the-fold sections once LCP stabilizes.  
   - Ensure hero glow utilities remain intact per `docs/design/system.md`.
3. **Longer-term** — Plan Phase 5 script deferrals (ClientRouter, AmbientAudio) after verifying CSS results.

## How to Continue
1. Read `docs/performance-speed-max-playbook.md` and the Phase sections in `docs/research/README.md`.  
2. Decide on a font/background experiment to tackle mobile LCP first; log results in `docs/seo/experiments.md`.  
3. When LCP target is achieved, move into Phase 4 tasks using the playbook prompts.  
4. Keep saving command outputs under `docs/research/artifacts/<date>-phase-*` folders and update this handoff (or create a new dated entry) for subsequent agents.

Everything needed to resume is in this file plus the artifacts folder. Ping `docs/seo/codex-vibe-seo-plan.json` for roadmap prompts if you need additional context.
