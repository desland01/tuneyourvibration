# Phase 1 Telemetry Brief — Performance Speed-Max Initiative

Generated by Codex (acting as Claude Code) on 2025-10-12 to guide baseline data capture before optimization work.

## Objectives & Success Metrics
- ☐ Record authoritative baselines for Core Web Vitals proxies (FCP, LCP, CLS, TBT) on homepage (`/`) and `/sound-healing-nepal/learn` using Lighthouse mobile + desktop runs against the current codebase.
- ☐ Capture bundle composition from `npm run build -- --verbose`, including total output size, top JS/CSS chunks, and hydrated island counts.
- ☐ Verify build health via `npm run check` and note any warnings/errors that could affect performance sprints.
- ☐ (Optional) Confirm search index compatibility by running `npm run postbuild` if assets allow; log success/failure.
- ☐ Store raw telemetry artifacts in `docs/research/artifacts/2025-10-12-phase-1/` (Lighthouse reports, command logs) and summarize highlights in tracking docs.

## Implementation Constraints
- Maintain alignment with `docs/design/system.md` (no configuration changes in Phase 1).
- Do not adjust source files; Phase 1 is read-only data collection unless build commands require temporary assets.
- Use Node 22 (`nvm use 22`) prior to all commands.
- Keep logs concise in docs (summaries + key numbers); large outputs belong in artifact files.

## Command & Data Checklist
1. ☐ `npm install` (only if node_modules absent; otherwise skip to avoid mutation).
2. ☐ `npm run check`  
   - Capture pass/fail status and notable warnings.
   - Store summarized results in `docs/research/README.md`.
3. ☐ `npm run build -- --verbose`  
   - Extract: total output size, largest JS bundle(s), CSS totals, hydrated islands count.
   - Save full console output to `docs/research/artifacts/2025-10-12-phase-1/build-log.txt`.
4. ☐ Optional `npm run postbuild`  
   - Note execution time and errors (if any). Store log in artifacts folder.
5. ☐ Lighthouse runs (mobile + desktop) against local preview:  
   - Start preview server (`npm run preview`) or serve `dist/` static if required.  
   - Use Lighthouse CLI or Chrome to export JSON/HTML reports.  
   - Record key metrics (Performance score, FCP, LCP, CLS, TBT, TTI) and save raw reports in artifacts folder.
6. ☐ WebPageTest (optional if automation available) — flag as TODO if not executed this phase.

## Touchpoints & Logging
- `docs/research/README.md`: add bullet summarizing Phase 1 telemetry completion with timestamps + link to artifact folder.
- `docs/seo/experiments.md`: update Performance Speed-Max Progress table (Phase 1 → ☑) and list key metrics.
- `docs/research/artifacts/2025-10-12-phase-1/`: store logs (`build-log.txt`, `check-log.txt`, `preview-log.txt`), Lighthouse reports (`lighthouse-home-mobile.json`, etc.), screenshots if captured.
- `docs/performance-speed-max-playbook.md`: no edits needed; reference when reporting status.

## Risks & Blockers
- Preview server + Lighthouse automation may require additional tooling; if CLI is unavailable, note manual run requirement and attach screenshots instead.
- Large build outputs could exceed memory on limited environments; if `npm run build` fails, document failure and retry with diagnostics before escalating.
- If third-party scripts require environment variables, ensure `.env` is configured; otherwise log missing config and proceed without metrics dependent on them.

## Follow-up Question Queue
- Do we have preferred Lighthouse configuration (e.g., `--throttling-method=provided`) for parity with earlier baselines? If unspecified, default to Lighthouse CLI defaults.
- Should WebPageTest runs target specific locations/devices for monitoring, or will Lighthouse suffice for Phase 1?
- Are there existing CI artifacts we need to align with to avoid duplication?

On completion, return to the playbook Phase 1 checklist and mark all items as done before advancing to Phase 2.
